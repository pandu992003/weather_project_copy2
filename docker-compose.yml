services:
  mcp-server:
    build: .
    container_name: mcp-server
    command: uvicorn server:app --host 0.0.0.0 --port 8000
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: always

  streamlit-ui:
    build: .
    container_name: streamlit-ui
    command: streamlit run agent_client.py --server.port 8501 --server.address 0.0.0.0
    env_file:
      - .env
    ports:
      - "8501:8501"
    depends_on:
      - mcp-server
    restart: always
